# Дизайн задачи 50 — Дашборд (референс 4)

Фаза 9. Выполняется **после логики задачи 50** (виджеты и показатели
MVP с фильтрами периода). Сетка главной страницы уже задана на шаге
`09-calendar-and-home.md`.

Что нужно от владельца: `docs/design/refs/04-dashboard.png`; решение по
графикам: нужна ли библиотека графиков (новая зависимость, требует
подтверждения) или достаточно простой вёрстки на цветах палитры;
пожелания по цветам графиков.

Уточнить перед запуском: какие виджеты реально реализованы в задаче 50
и как считаются данные.

```
Прочитай CLAUDE.md, docs/26-ui-designe-plan.md (раздел «Фаза 9»),
docs/15-dashboard-and-analytics.md, docs/16-pwa-and-mobile.md,
docs/design/01-design-system.md, docs/design/refs/04-dashboard.png.
Найди виджеты дашборда из задачи 50 (Livewire-компоненты и шаблоны) и
сетку главной страницы из шага календаря. Если нужен навык оформления
графиков, используй общий принцип: цвета из палитры дизайн-системы,
читаемость в обеих темах.

Этап 1 — спецификация, кода нет:
1. Сопоставь блоки референса с показателями MVP (новые заявки за период,
   сделки по этапам, суммы по этапам, сумма оплаченных предоплат,
   проведённые мероприятия, отказы, конверсия, ближайшие мероприятия,
   новые заявки сегодня, просроченные задачи). Для каждого: вид (число,
   плитка, список, диаграмма), место в сетке, цвет.
2. Фильтр периода (сегодня, неделя, месяц, квартал, произвольный):
   расположение и вид, вид на телефоне.
3. Графики: способ реализации (вёрстка на цветах палитры без
   библиотеки — рекомендую; либо библиотека графиков — только с моим
   подтверждением как новая зависимость), доступность (подписи и текстовая
   альтернатива числам), поведение при отсутствии данных.
4. Схема для телефона: блоки один под другим, важные показатели
   выше.
Список файлов, жди подтверждения.

Этап 2 — реализация (только вёрстка и стили):
1. Оформи виджеты и сетку по подтверждённой схеме на компонентах
   дизайн-системы; цвета этапов из общих переменных.
2. Агрегации и запросы не менять; тесты не ослаблять.
3. Числа форматируются (разделители тысяч, валюта без обрезания на
   телефоне), пустые состояния («Нет данных за период») оформлены.
4. Без горизонтальной прокрутки страницы; обе темы.

В конце: npm run build, composer check (и в Sail), git status и git diff,
ничего не коммить. Дай чек-лист: каждый виджет с данными и без,
смена периода, компьютер и iPhone, обе темы, длинные числа.
```
