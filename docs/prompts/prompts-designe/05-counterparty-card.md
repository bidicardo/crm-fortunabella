# Дизайн задачи 22 — Карточка и форма контрагента (референс 7)

Фаза 3. Выполняется **после логики задачи 22** (CRUD контрагента с
несколькими контактными лицами уже работает на компонентах
дизайн-системы) и до канбана (задача 23).

Что нужно от владельца: `docs/design/refs/07-counterparty-card.png`;
пометки, что в референсе относится к контрагенту, а что — к сделке.

Уточнить перед запуском: названия компонентов Livewire и шаблонов
контрагента (в момент написания промпта они ещё не созданы), выполнены ли
Д1, Д2, 19а и Д3.

```
Прочитай CLAUDE.md, docs/26-ui-designe-plan.md, docs/09-counterparties.md,
docs/design/01-design-system.md, docs/design/refs/07-counterparty-card.png,
docs/16-pwa-and-mobile.md и готовую карточку клиента
(resources/views/livewire/client-show.blade.php) — компоновка
контрагента должна быть с ней согласована, потому что это образец,
принятый на шаге 19а. Задача 22 выполнена: найди её компоненты Livewire и
шаблоны (форма, карточка, список контактных лиц).

Этап 1 — спецификация, кода нет: сопоставь референс с полями
контрагента из docs/09-counterparties.md (название, тип — свободный
текст, телефон, email, социальная сеть, условия сотрудничества, дата
начала сотрудничества, сайт, Telegram, адрес, связанные сделки,
документы, история). Для каждого блока референса: берём / не берём /
заменяем. Отдельно опиши блок «Контактные лица»: несколько лиц,
добавление, редактирование, удаление с подтверждением, поля лица (ФИО,
должность, телефон, email, соцсеть, удобное время, примечание). Схема
для компьютера и телефона; где будущие блоки «Сделки» (Фаза 4) и
«Документы» (Фаза 7). Список файлов, жди подтверждения.

Этап 2 — реализация (только вёрстка):
1. Карточка и форма контрагента, список контактных лиц — на компонентах
   дизайн-системы, по подтверждённой схеме.
   Страница создания повторяет компоновку карточки (образец — карточка и
   страница создания клиента, задача 19а: сетка на компьютере, поля по два
   в строке, длинные тексты — отдельным блоком, служебные даты справа).
   Редактирование — прямо в карточке по одному полю щелчком
   (`<x-ui.editable-field>`, методы edit/save/cancel, как в ClientShow);
   отдельной страницы редактирования нет.
2. Сохранить логику: заголовок раздела «Контрагент» в верхней панели,
   история изменений (тот же блок, что у клиента: 3 → 10 записей с
   прокруткой внутри, «Свернуть»), экранирование вывода, стрелка «Назад»
   ведёт в родительский раздел автоматически.
3. Без горизонтальной прокрутки на телефоне; обе темы.
4. PHP-компоненты, маршруты, данные и тексты не менять; если референс
   требует нового поля, сначала спроси.
5. Повторяющиеся с карточкой клиента части бери из общих компонентов, не
   копируй разметку.

Тесты не ослаблять; поправки ожиданий на разметку опиши.

В конце: npm run build, composer check (и в Sail), git status и git diff,
ничего не коммить. Дай чек-лист: карточка полная и пустая, несколько
контактных лиц, добавление и удаление лица, длинные значения, история;
компьютер и iPhone, обе темы.
```
