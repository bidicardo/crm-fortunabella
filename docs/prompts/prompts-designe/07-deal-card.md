# Дизайн задачи 27 — Карточка и форма сделки (референс 7)

Фаза 4. Выполняется **после логики задачи 27** (CRUD сделки, клиент
обязателен, контрагент необязателен) и до канбана сделок.

Что нужно от владельца: `docs/design/refs/07-deal-card.png`; пометки, как
сгруппировать многочисленные поля сделки по блокам и колонкам.

Уточнить перед запуском: названия компонентов Livewire и шаблонов сделки,
выполнены ли предыдущие дизайн-шаги (Д1–Д3, 19а, дизайн контрагентов),
как реализованы предупреждения о дате и предоплате (задачи 29–30).

```
Прочитай CLAUDE.md, docs/26-ui-designe-plan.md (раздел «Фаза 4»),
docs/10-deals.md, docs/05-business-rules.md (разделы «Сделки» и
«Предоплата»), docs/06-sales-pipeline.md, docs/design/01-design-system.md,
docs/design/refs/07-deal-card.png, docs/16-pwa-and-mobile.md, а также
готовые карточки клиента и контрагента — компоновка сделки должна быть
с ними согласована. Найди компоненты Livewire и шаблоны сделки из
задачи 27.

Этап 1 — спецификация, кода нет: сопоставь референс с полями сделки
(название, клиент, контрагент, сумма, дата проведения, типы столов —
множественный выбор, длительность — единичный выбор с вариантом
«другое», характер мероприятия, количество гостей, адрес, источник лида,
предоплата (сумма, статус, держатель), договор, кто продал, задачи,
примечания, история). Для каждого блока референса: берём / не берём /
заменяем. Опиши: группировку полей по блокам и колонкам для форм и
карточки, выбор клиента и контрагента (поиск), индикатор занятой даты
(жёлтый треугольник с восклицательным знаком в строке поля даты, при
наведении или нажатии — предупреждение о другом мероприятии),
предупреждение о неоплаченной предоплате при переходе в «Бронь», цвет
и метку этапа сделки, схему для телефона (форма длинная: секции,
удобные выпадающие списки и переключатели). Место под будущие блоки
«Задачи» (Фаза 6) и «Документы» (Фаза 7). Список файлов, жди
подтверждения.

Этап 2 — реализация (только вёрстка):
1. Форма и карточка сделки на компонентах дизайн-системы по
   подтверждённой схеме.
   Страница создания повторяет компоновку карточки (образец — карточка и
   страница создания клиента, задача 19а: сетка на компьютере, поля по два
   в строке, длинные тексты — отдельным блоком, служебные даты справа).
   Редактирование — прямо в карточке по одному полю щелчком
   (`<x-ui.editable-field>`, методы edit/save/cancel, как в ClientShow);
   отдельной страницы редактирования нет.
2. Сохранить логику и доступность: предупреждения не блокируют
   сохранение, история (общий блок), заголовок раздела «Сделка» в
   верхней панели, стрелка «Назад» в родительский раздел автоматически.
3. Без горизонтальной прокрутки на телефоне, обе темы.
4. PHP-компоненты, маршруты, данные и тексты не менять; если референс
   требует нового поля, сначала спроси.

Тесты не ослаблять; поправки ожиданий на разметку опиши.

В конце: npm run build, composer check (и в Sail), git status и git diff,
ничего не коммить. Дай чек-лист: создание и редактирование сделки,
занятая дата (индикатор), «Бронь» с неоплаченной предоплатой,
множественный выбор столов, длинная форма на iPhone; обе темы.
```
