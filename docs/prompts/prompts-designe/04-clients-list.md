# Д3 — Список клиентов (референс 2)

Выполняется после 19а, до задачи 20. Решение о виде таблицы на
телефоне здесь принимается один раз и действует для всех списков
(контрагенты, задачи, документы).

Что нужно от владельца: `docs/design/refs/02-clients-list.png`; ответ,
как показывать таблицу на телефоне: таблица с прокруткой внутри блока
(как сейчас) или карточки-строки.

```
Прочитай CLAUDE.md, docs/26-ui-designe-plan.md (раздел «Д3»),
docs/08-clients.md (раздел «База клиентов»), docs/design/01-design-system.md,
docs/design/refs/02-clients-list.png, docs/16-pwa-and-mobile.md.
Д1, Д2 и 19а выполнены. Посмотри app/Livewire/ClientTable.php,
resources/views/livewire/client-table.blade.php и tests/Feature/
ClientTableTest.php.

Этап 1 — спецификация, кода нет: сопоставь референс со списком.
Для каждого элемента референса (панель поиска, фильтры, кнопки,
заголовок таблицы, строки, метки, аватары/инициалы, действия в строке,
пагинация, пустое состояние) укажи: берём / не берём / заменяем. Учти
существующие функции, которые должны остаться: поиск без Enter, фильтр
по типу, переключатель «Только архивные», сортировка по имени, «Создан»,
«Изменён», выбор колонок (хранится в localStorage), пагинация по 25,
сохранение параметров в адресе, метка «архив». Опиши вид на телефоне
(по ответу владельца: прокрутка внутри блока либо карточки-строки) и
общий принцип для будущих списков. Список файлов, жди подтверждения.

Этап 2 — реализация (только вёрстка списка):
1. Переверстай client-table.blade.php по подтверждённой схеме на
   компонентах дизайн-системы; общий компонент таблицы/списка
   сделай пригодным для повторного использования в других списках,
   но без лишних абстракций.
2. Сохрани: адреса и параметры (q, type, archived, sort, dir), ссылки
   на карточки, доступность (aria-sort, подписи), пустое состояние
   «Клиентов не найдено», кнопку «Новый клиент».
3. Поиск, фильтр и переключатель на телефоне удобны: поле поиска на
   всю ширину, шрифт 16 px, кнопки не меньше 44 px.
4. Не менять PHP-компонент, запросы, тексты, поведение поиска и
   пагинации. Если для референса нужны новые данные (например, число
   сделок), сначала спроси.

Тесты не ослаблять; поправки ожиданий на разметку опиши.

В конце: npm run build, composer check (и в Sail), git status и git diff,
ничего не коммить. Дай чек-лист: поиск, фильтр, архив, сортировка,
колонки (запоминание после F5), пагинация, пустое состояние, длинные
значения; компьютер и iPhone, обе темы, без горизонтальной прокрутки
страницы.
```
