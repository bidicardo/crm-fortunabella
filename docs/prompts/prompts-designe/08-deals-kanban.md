# Дизайн задачи 28 — Канбан сделок (референс 8)

Фаза 4. Выполняется **после логики задачи 28** (этапы, перетаскивание,
суммы и количество в колонках, свободные переходы). Использует решения
канбана контрагентов (`06-counterparties-kanban.md`).

Что нужно от владельца: тот же `docs/design/refs/08-kanban.png` (если
для сделок есть отдельный вариант, приложить его); цвета этапов сделок.

Уточнить перед запуском: выполнен ли дизайн канбана контрагентов и какие
общие компоненты колонки и карточки уже есть; как в задаче 28 считаются
суммы и количество.

```
Прочитай CLAUDE.md, docs/26-ui-designe-plan.md (раздел «Фаза 4»),
docs/06-sales-pipeline.md, docs/10-deals.md (разделы «Календарь» и
«Цвета»), docs/16-pwa-and-mobile.md (раздел «Канбан на телефоне»),
docs/design/01-design-system.md, docs/design/refs/08-kanban.png. Найди
канбан сделок из задачи 28 (Livewire-класс, шаблон, drag-and-drop) и
готовый дизайн канбана контрагентов: общие компоненты колонки, карточки
и счётчика нужно переиспользовать, а не копировать.

Этап 1 — спецификация, кода нет:
1. Что общее с канбаном контрагентов, что отличается: в заголовке колонки
   количество и сумма сделок; в карточке сделки — название, клиент,
   дата, сумма, метки (предоплата, предупреждение о занятой дате),
   цвет этапа.
2. Цвета этапов (Новая заявка, В работе, Бронь, Проведена, Отказ) из
   палитры дизайн-системы; те же цвета используются в календаре
   (`09-calendar-and-home.md`), поэтому они должны браться из одних
   переменных.
3. Телефон: тот же режим, что у контрагентов; кнопка смены этапа,
   учитывающая свободные переходы (в любую стадию), предупреждение при
   переходе в «Бронь» без оплаченной предоплаты (не блокирует).
4. Как показывать сделки со статусом «Отказ» (по документам в календаре
   они скрыты по умолчанию; для канбана — по логике задачи 28).
Список файлов, жди подтверждения.

Этап 2 — реализация (только вёрстка и стили):
1. Оформи колонки и карточки сделок на общих компонентах.
2. Не ломать перетаскивание и его тесты, не менять идентификаторы,
   на которые опираются drag-and-drop и Livewire.
3. Суммы и количество в заголовках колонок читаются на телефоне
   (числа не обрезаются, форматирование с разделителями).
4. Без горизонтальной прокрутки всей страницы; обе темы.
5. Логику, данные и тексты не менять; новые зависимости не ставить.

В конце: npm run build, composer check (и в Sail), git status и git diff,
ничего не коммить. Честно напиши, что не проверено без браузера, и дай
чек-лист: перетаскивание мышью и пальцем, кнопка смены этапа, суммы и
счётчики после перемещения, переход в «Бронь» с неоплаченной
предоплатой, пустая колонка, много карточек; обе темы.
```
