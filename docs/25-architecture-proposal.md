# Архитектурное предложение

Документ описывает предлагаемую техническую архитектуру Fortuna CRM на
основе требований (`docs/01`–`docs/22`), ограничений хостинга
(`docs/23-hosting-constraints.md`) и реального примера интеграции с
лендингом (`docs/24-exaple-sendphp-from-site.md`, уже отражён в
`docs/13-landing-api.md`).

Решения этого документа приняты и реализуются: стек установлен, выполнены
Фазы 0, 1, 1а, задачи 13–19 Фазы 2, дизайн-система Д1 и страница входа Д2 (см. раздел
«Статус выполнения» в конце). Если реализация отличается от первоначального предложения, ниже
это отмечено явно (например, вход сделан без Laravel Breeze).

Общий принцип выбора: не закладывать инфраструктуру сложнее, чем
позволяет shared-хостинг REG.RU (`docs/23`), и такую, которую сможет
самостоятельно поддерживать один начинающий разработчик, опираясь на
Claude Code — то есть минимум "движущихся частей", максимум
стандартных, хорошо задокументированных решений.

---

## 1. Backend-стек

**Решение**: PHP 8.4 (предпочтительно) или PHP 8.5.7 (alt), фреймворк
**Laravel 13**.

**Зачем.** Версия PHP и Laravel уже зафиксированы в `docs/23` как
требование хостинга. Laravel даёт "из коробки" почти всё, что нужно
CRM: аутентификацию, валидацию, миграции БД, очереди на базе БД,
планировщик задач, файловое хранилище, ORM. Это резко сокращает объём
кода, который придётся писать и поддерживать вручную.

**Плюсы**
- Огромное сообщество и документация — Claude Code хорошо знает
  Laravel-паттерны, меньше риск "придуманных" решений.
- Все нужные для MVP подсистемы уже встроены, не нужно собирать стек
  из отдельных библиотек.
- Простая модель развёртывания на обычном PHP-хостинге (без отдельного
  рантайма/процесса).

**Минусы**
- Монолитная архитектура — при сильном росте потребует более
  вдумчивого разделения на модули (для MVP это не проблема).
- Не все Composer-пакеты гарантированно совместимы с альтернативной
  версией PHP (8.5.7 alt) — нужно пройти чек-лист совместимости из
  `docs/23` перед стартом.

---

## 2. Frontend-стек

**Решение**: TALL-стек — **T**ailwind CSS + **A**lpine.js + **L**aravel
+ **L**ivewire. Без отдельного SPA-фреймворка (React/Vue) и без
отдельного JSON API для интерфейса.

**Зачем.** Laravel рендерит HTML-страницы (Blade), Livewire добавляет
интерактивность (обновление частей страницы без полной перезагрузки)
прямо на PHP, без необходимости писать и синхронизировать отдельный
JS-стейт и отдельный REST/JSON API только ради интерфейса. Для
drag-and-drop канбана — библиотека SortableJS, подключаемая к
Livewire-компоненту; для календаря — готовый JS-виджет FullCalendar.

**Плюсы**
- Один язык бэкенда и фронтенда (PHP + немного JS) — проще
  поддерживать одному разработчику.
- Нет дублирования бизнес-логики (валидация, права доступа) между
  backend и отдельным frontend-приложением.
- Меньше собираемых артефактов и зависимостей (нет отдельного
  npm-приложения с собственным роутингом, стейт-менеджером и т. д.).

**Минусы**
- Меньше гибкости для очень сложных, "нативных" по ощущениям
  интерфейсов — для CRM среднего размера это не критично.
- Канбан и календарь всё равно требуют аккуратной работы с JS-виджетами
  поверх Livewire (не "бесплатно").

---

## 3. База данных

**Решение**: MySQL или MariaDB (версия — та, что реально доступна в
ISPmanager, уточнить перед стартом), кодировка `utf8mb4`, движок
InnoDB.

**Зачем.** Требование хостинга (`docs/23`) — на REG.RU Host-0 доступны
только MySQL-совместимые базы данных, без ограничения по количеству.

**Плюсы**
- Никакого выбора не требуется — вариант единственный и уже подтверждён
  ограничениями хостинга.
- InnoDB даёт транзакции и внешние ключи — важно для целостности связей
  Client/Deal/Counterparty.

**Минусы**
- MySQL/MariaDB менее строг по умолчанию, чем PostgreSQL (например, в
  обработке дат/пустых строк) — нужно явно включать строгий режим
  (`strict mode`) в конфигурации Laravel.

---

## 4. ORM и миграции

**Решение**: Eloquent ORM (встроен в Laravel) + стандартные
Laravel-миграции.

**Зачем.** Eloquent не требует установки — он уже часть Laravel.
Миграции дают читаемую, версионируемую историю изменений схемы прямо в
Git — удобно смотреть, что именно поменялось, и удобно поэтапно
наращивать модель данных вместе с Claude Code, задача за задачей.

**Плюсы**
- Ноль дополнительных зависимостей.
- Миграции = самодокументирующаяся история схемы, легко откатить.
- Много готовых паттернов (factories, seeders) для тестовых данных.

**Минусы**
- ORM может маскировать N+1-запросы, если не использовать eager
  loading (`with()`) — стоит с самого начала внести это как соглашение
  в код-ревью/тесты.

---

## 5. Авторизация

**Решение**: встроенная сессионная аутентификация Laravel — без открытой
регистрации. Laravel Breeze **не используется**: вход, выход, приглашения и
блокировка сделаны собственными Livewire-компонентами (меньше лишнего кода
и маршрутов, например регистрации и сброса пароля). Отдельно
— собственная модель `Invite` с одноразовым токеном для регистрации
сотрудников. Для эндпоинта лендинга — отдельная простая проверка
секретного токена в заголовке запроса (middleware), без OAuth/Sanctum.

**Зачем.** CRM — не публичный сервис с множеством внешних API-клиентов,
а внутренний инструмент с одним доверенным внешним клиентом (лендинг).
Сессионная авторизация — самый простой и наименее подверженный ошибкам
вариант для веб-интерфейса; токен в заголовке — самый простой
достаточный вариант для одного доверенного клиента.

**Плюсы**
- Минимум кода и зависимостей, всё встроено в Laravel.
- Модель Invite прямо соответствует описанному в `docs/03` флоу
  (одноразовый токен, самостоятельная установка пароля).

**Минусы**
- Секретный токен лендинга нужно вручную ротировать при компрометации
  (нет автоматического refresh, как в OAuth) — для одного клиента это
  приемлемо.

---

## 6. Хранение документов

**Решение**: Laravel Filesystem с приватным диском вне `public/`.
Скачивание — только через авторизованный контроллер, который проверяет
права и стримит файл.

**Зачем.** Документы (договоры) не должны быть доступны по прямой
ссылке без авторизации (`docs/19`, `docs/23`). Локальный приватный диск
не требует внешнего облачного хранилища и работает "из коробки" на
shared-хостинге.

**Плюсы**
- Не нужен S3 или другой облачный сервис для MVP — меньше внешних
  зависимостей и затрат.
- Абстракция дисков Laravel позволяет в будущем переключиться на
  S3-совместимое хранилище, поменяв только конфигурацию, а не код.

**Минусы**
- Файлы хранятся на одном сервере — точка отказа. Нужен регулярный
  cron-бэкап каталога документов вместе с дампом БД (это уже отмечено
  как требование в `docs/19` и `docs/23`).

---

## 7. Очередь задач и планировщик уведомлений

**Решение**: database-драйвер очереди Laravel (`QUEUE_CONNECTION=database`,
задачи хранятся в таблице `jobs`) + планировщик Laravel, запускаемый
через один cron раз в минуту (`php artisan schedule:run`), без
постоянно работающего воркера.

**Зачем.** Хостинг не гарантирует постоянно работающий процесс
(`docs/23`) и явно запрещает Redis/RabbitMQ/Celery. Cron + БД —
единственный совместимый вариант: планировщик по расписанию находит,
что нужно отправить (заявки, просроченные задачи, напоминания о
мероприятиях), и обрабатывает очередь короткими прогонами
(`queue:work --stop-when-empty`).

**Плюсы**
- Не требует дополнительной инфраструктуры (брокеров сообщений,
  воркеров).
- Полностью укладывается в ограничения shared-хостинга.

**Минусы**
- Задержка уведомлений ограничена интервалом cron (обычно не чаще раза
  в минуту) — для CRM это не критично, не требуется "реальное время".

---

## 8. Способ реализации PWA

**Решение**: стандартный `manifest.json` + простой service worker,
кэширующий статику (иконки, app shell), без сложной офлайн-логики.

**Зачем.** `docs/16` явно требует установку на iPhone через Safari и
работу как приложения с домашнего экрана, но при этом "достаточно
работы при наличии интернета" — то есть не нужен полноценный офлайн-режим,
а значит service worker можно сделать простым, без сложной синхронизации
данных.

**Плюсы**
- Минимальный service worker — меньше кода, меньше багов с
  устаревшим кэшем.
- Не требует отдельного фреймворка для PWA — Laravel просто отдаёт эти
  файлы как статику.

**Минусы**
- Если в будущем понадобится офлайн-режим — потребуется отдельная
  доработка (сознательно отложено, см. `docs/02-mvp-scope.md`).

---

## 9. Способ push-уведомлений для iPhone

**Решение**: Web Push (VAPID) через PHP-библиотеку `minishlink/web-push`
(указана как рекомендованная в `docs/23`). Подписки хранятся в таблице
`push_subscriptions`, отправка идёт теми же cron-командами, что и
остальные уведомления (см. п. 7).

**Зачем.** Это единственный стандартный способ слать push в PWA на
iPhone без постоянного серверного процесса — обычный HTTP-запрос к
push-сервису Apple/браузера, инициируемый по расписанию.

**Плюсы**
- Не требует стороннего сервиса рассылки (Firebase и т.п.).
- Укладывается в ограничения shared-хостинга (обычный исходящий HTTPS-запрос).

**Минусы**
- iOS поддерживает push только для PWA, установленной на домашний
  экран, начиная с определённой версии iOS, и только после явного
  разрешения пользователя (`docs/16`) — это ограничение платформы, не
  реализации.
- Если push не пришёл (пользователь не разрешил/отключил), CRM должна
  продолжать работать — Telegram остаётся резервным каналом.

---

## 10. Способ интеграции с Telegram

**Решение**: переиспользовать паттерн из `docs/24-exaple-sendphp-from-site.md`
— исходящие HTTP-запросы к Telegram Bot API (`sendMessage`) через cURL
из PHP, обёрнутые в отдельный сервис/канал уведомлений Laravel.
Запускается теми же cron-командами, без long polling и без постоянного
процесса бота.

**Зачем.** Уже есть рабочий, проверенный код именно этого подхода
(текущий скрипт на лендинге). CRM нужно только отправлять сообщения
(не принимать команды от пользователей в боте), поэтому webhook/long
polling не требуются вовсе.

**Плюсы**
- Код уже существует и проверен в проде (лендинг), логику можно
  напрямую перенести в сервис уведомлений CRM.
- Минимум инфраструктуры — обычный исходящий HTTP-запрос.

**Минусы**
- Если понадобится двусторонний бот (принимать команды) — потребуется
  либо webhook (нужен публичный HTTPS-эндпоинт, что не проблема на
  этом хостинге), либо polling (для которого действительно нужен
  постоянный процесс) — это не входит в MVP.

---

## 11. Docker-окружение для локальной разработки и будущего VPS

**Решение**: **Laravel Sail** для локальной разработки (PHP + MySQL +
Mailpit в контейнерах) — используется только на компьютере
разработчика. В продакшене на REG.RU Docker **не используется**, как
того требует `docs/23` — там обычный PHP-FPM.

**Зачем.** Sail даёт одинаковое окружение для всех, кто разрабатывает
проект, без ручной установки PHP/MySQL на каждой машине, и это
официальный, хорошо документированный инструмент Laravel. При этом он
не влияет на продакшен-деплой, который остаётся простым upload
PHP-кода на shared-хостинг.

**Плюсы**
- Быстрый старт локальной разработки одной командой.
- Не создаёт риска "а вдруг понадобится Docker на проде" — эти два
  окружения осознанно разделены.

**Минусы**
- Два разных способа запуска (Docker локально, обычный PHP-FPM на
  проде) нужно держать в голове — расхождения версий PHP/MySQL между
  Sail и реальным хостингом нужно сверять вручную (чек-лист есть в
  `docs/23`).

**На будущее.** Если проект перерастёт shared-хостинг и понадобится
переезд на VPS — тот же `docker-compose.yml` из Sail можно развить в
production-конфигурацию (добавить Nginx, супервизор для очередей и
т. д.). Это осознанно не часть MVP.

---

## 12. Структура папок проекта

Стандартная структура Laravel-приложения, доменная логика CRM
раскладывается так:

```
app/
  Models/            Eloquent-модели: User, Invite, Client, Counterparty,
                      CounterpartyContact, Deal, Task, Document,
                      LeadSubmission, ActivityLog, Notification,
                      PushSubscription
  Models/Concerns/    Трейты моделей (LogsActivity — история изменений)
  Enums/              Перечисления (Role, ClientLegalType)
  Rules/              Правила валидации (RussianPhone)
  Support/            Вспомогательные классы (Navigation — меню и стрелка «Назад»)
  Livewire/           Компоненты экранов. Готовы: Home, Login, AcceptInvite,
                      Users, ClientTable, ClientForm, ClientShow, ClientMerge.
                      Планируются: DealsKanban, CounterpartiesKanban,
                      DealCard, Dashboard, Calendar
  Services/           Доменные сервисы без UI. Готовы: PhoneNormalizer,
                      DuplicateFinder, ClientMergeService.
                      Планируются: LeadImportService, TelegramNotifier,
                      WebPushSender
  Notifications/      Классы уведомлений Laravel (email/telegram/push-каналы)
  Http/
    Controllers/      Тонкие контроллеры (в основном не-Livewire маршруты:
                      API лендинга, скачивание документов, auth)
    Middleware/        Проверка токена лендинга, roles/permissions
config/
  navigation.php      Единый источник левого меню, нижней панели и правила
                      «Назад» (только массивы, без замыканий)
  crm.php             Настройки CRM (срок приглашения и т. п.)
database/
  migrations/         История схемы БД
  factories/          Фейковые данные для тестов (без реальных клиентов)
  seeders/
resources/
  views/              Blade-шаблоны и Livewire-вьюхи
routes/
  web.php             Веб-интерфейс (сессионная авторизация)
  api.php             Эндпоинт приёма заявок с лендинга
tests/
  Feature/            Тесты по сценариям (HTTP, Livewire-компоненты)
  Unit/               Тесты чистой логики (нормализация телефона и т.д.)
```

**Зачем такая структура.** Она стандартна для Laravel — при появлении
новой функциональности Claude Code (и человек-разработчик) сразу знает,
куда класть новый код, не изобретая свою организацию проекта.

---

## 13. Стратегия тестирования

**Решение**: Pest (или PHPUnit — оба штатно доступны в Laravel).
Feature-тесты — на уровне HTTP/Livewire-сценариев (приглашение и вход,
приём заявки с лендинга, переходы по канбану, слияние дублей клиентов,
загрузка/скачивание документов). Unit-тесты — на чистую логику
(нормализация телефона, поиск дублей). Тестовая БД — SQLite in-memory
для скорости, если код не полагается на MySQL-специфичные функции.
Никаких реальных клиентских данных в тестах — только фабрики/фейкер
(по правилу `CLAUDE.md`).

**Зачем.** Каждая задача из списка ниже должна оставлять после себя
минимум один проверяемый тест — это прямо соответствует правилу
"ponytail" в `CLAUDE.md`/системных инструкциях (нетривиальная логика не
считается законченной без теста) и защищает от регрессий при
дальнейшей пошаговой разработке через Claude Code.

**Плюсы**
- Встроено в Laravel, не требует дополнительной настройки CI с нуля.
- Feature-тесты по сценариям — надёжная защита при поэтапной разработке
  большими шагами через Claude Code.

**Минусы**
- SQLite в тестах может скрыть расхождения с реальным MySQL в редких
  случаях (например, коллации, специфичные функции даты) — при
  подозрении на такие расхождения тест стоит прогонять и на MySQL.

---

## План задач для Claude Code

Каждая задача — самостоятельный, проверяемый шаг размером примерно в
одну сессию. Задачи идут по возрастанию, от фундамента к полной CRM;
каждая следующая опирается на предыдущие. После задач с моделями/логикой
всегда идёт задача с тестами на неё же — не откладывать тесты на потом.

### Фаза 0 — Bootstrap
1. Создать проект Laravel 13, настроить `.env.example`, базовый README
   для разработчика (как поднять окружение).
2. Настроить Laravel Sail для локальной разработки (PHP + MySQL +
   Mailpit), проверить `sail up`.
3. Подключить TALL-стек: Tailwind CSS, Alpine.js, Livewire; настроить
   сборку через Vite.
4. Настроить Laravel Pint (форматирование) и Pest как тестовый фреймворк.
5. Добавить базовый скрипт/CI-шаг «прогнать тесты и линтер одной командой».

### Фаза 1 — Пользователи и доступ
6. Миграция и модель `User` (роль: creator/member, статус блокировки).
7. Экраны входа/выхода (собственные Livewire-компоненты, без Breeze), без открытой регистрации.
8. Миграция и модель `Invite` (токен, срок действия, статус использования).
9. Флоу приглашения: создатель генерирует ссылку → сотрудник переходит
   → сам задаёт пароль → токен становится недействительным.
10. Блокировка/разблокировка участника создателем.
11. Middleware/policy для различия прав «создатель» vs «участник»
    (бинарно, без гранулярных чек-боксов — см. открытый вопрос в
    `docs/21-open-questions.md`).
12. Feature-тесты на весь флоу приглашения, входа и блокировки.

### Фаза 1а — Каркас интерфейса
12.1. Левое меню и верхняя панель: боковая панель с группами (Главная;
    Продажи: Сделки, Клиенты; Партнёры: Контрагенты, Действующие
    контрагенты; Работа: Задачи, Календарь; Документы), сворачивание до
    иконок с запоминанием, название текущего раздела вверху, меню
    пользователя (профиль, тёмная тема, «Пользователи» только
    создателю, «Выйти»). Заменяет временную шапку из Фазы 0–1.
    Пункты показываются только для реализованных разделов. Также:
    отдельный компактный layout для страниц входа и приглашения, стрелка
    «Назад» слева от названия раздела (ведёт в родительский раздел,
    `docs/17-ui-screens.md`), единый файл меню `config/navigation.php`.
12.2. Нижняя панель вкладок на телефоне (Главная, Сделки, Клиенты,
    Задачи, Ещё) вместо левого меню на узком экране, с учётом
    безопасной зоны iPhone. Детальная мобильная адаптация экранов —
    по-прежнему задача 54.

Описание — `docs/17-ui-screens.md` (раздел «Навигация и общий каркас»).

### Фаза 2 — Клиенты
13. Миграция и модель `Client` (поля из `docs/08-clients.md`).
14. Сервис нормализации телефона (11 цифр → E.164, правило из
    `docs/13-landing-api.md`) + unit-тесты.
15. CRUD клиента: создание, редактирование, карточка (роль клиента —
    свободный текст).
16. Список клиентов: таблица, поиск без Enter, фильтр по типу, режим
    «Только архивные», сортировка, выбор отображаемых колонок,
    пагинация по 25 (Livewire-компонент).
17. Обнаружение дублей при создании клиента (по телефону/email,
    сервис `DuplicateFinder`) — предупреждение с переходом к
    существующей карточке или продолжением создания.
18. Слияние дублей клиентов: перенос сделок/задач/документов/истории на
    основную карточку, архивирование дубля (доступно любому пользователю
    CRM — см. `docs/18-deduplication.md`).
19. История изменений клиента (универсальный `ActivityLog`, трейт
    `LogsActivity`, события created/updated/merged; на карточке 3 записи,
    «Показать ещё» — до 10 с прокруткой внутри блока).
Дизайн-задачи Фазы 2 (порядок и обоснование — `docs/26-ui-designe-plan.md`,
референсы — `docs/design/`). Выполняются после задачи 19 и до задачи 20:
Д1. Дизайн-система (референс 1): палитра, шрифты, иконки, набор
    Blade-компонентов, перенос уже готовых экранов на компоненты.
Д2. Страница входа, регистрации по приглашению и «Ссылка
    недействительна» (референс 5).
19а. Компоновка карточки клиента (референс 6): расположение полей и
    блоков; образец для карточек сделки и контрагента.
Д3. Список клиентов (референс 2): расположение фильтров и таблицы, вид
    на телефоне (решение действует для всех списков).
20. Feature-тесты клиентов, поиска дублей и слияния (сквозной тест и
    проверка критериев приёмки раздела «Клиенты»); выполняется после
    Д3, по окончательному виду.

### Фаза 3 — Контрагенты
21. Миграции и модели `Counterparty` + `CounterpartyContact`.
22. CRUD контрагента с несколькими контактными лицами (дизайн по
    референсу 7).
23. Канбан контрагентов: этапы, drag-and-drop, счётчики карточек
    (дизайн по референсу 8; общий для канбана сделок).
24. База действующих контрагентов (фильтр по этапу «Сотрудничаем»).
25. Feature-тесты контрагентов и канбана.

### Фаза 4 — Сделки
26. Миграция и модель `Deal` (все поля из `docs/10-deals.md`, включая
    поле «прибыль», добавленное по `docs/21-open-questions.md`).
27. CRUD сделки: клиент обязателен, контрагент необязателен (дизайн по
    референсу 7).
28. Канбан сделок: этапы, drag-and-drop, суммы и количество в колонках,
    свободные переходы между любыми стадиями без блокировки (дизайн по
    референсу 8).
29. Предупреждение о совпадении даты мероприятия (индикатор на поле
    даты, без блокировки сохранения).
30. Предупреждение о неоплаченной предоплате при переходе в «Бронь»
    (без блокировки).
31. История изменений сделки.
32. Feature-тесты канбана и бизнес-правил сделок (даты, предоплата,
    свободные переходы).

### Фаза 5 — Интеграция с лендингом
33. Эндпоинт `POST /api/landing/lead`: проверка секретного токена,
    honeypot-поля, rate limit через БД (не временные файлы — см.
    `docs/13-landing-api.md`).
34. Обработка заявки: нормализация телефона → поиск клиента → создание
    клиента и сделки либо только сделки для существующего клиента.
35. Журнал `LeadSubmission` (сырые данные заявки, статус обработки,
    ссылка на созданную сделку, факт согласия на обработку данных).
36. Feature-тесты API лендинга: успешная заявка, повторный телефон,
    невалидные данные, honeypot, превышение rate limit.

### Фаза 6 — Задачи и календарь
37. Миграция и модель `Task` (поля и статусы из
    `docs/11-tasks-and-calendar.md`).
38. CRUD задач + фильтр по статусу, связи со сделкой/клиентом/контрагентом.
39. Единый календарь (переключатели «мероприятия»/«задачи») и
    мини-календарь на главной странице (дизайн по референсу 3; здесь же
    фиксируется сетка главной страницы по референсу 4).
40. Визуальный индикатор конфликта дат в календаре сделок.
41. Feature-тесты задач и календаря.

### Фаза 7 — Документы
42. Миграция и модель `Document` (включая поле «дата подписания» —
    решение из `docs/21-open-questions.md`), приватный диск для файлов.
43. Загрузка/скачивание/удаление документа (с подтверждением удаления)
    из карточек сделки, клиента и контрагента.
44. Валидация типа (.pdf/.doc/.docx) и размера файла (лимит 10 МБ).
45. Feature-тесты документов, включая проверку, что файл недоступен без
    авторизации.

### Фаза 8 — Уведомления и Telegram
46. Миграция и модель `Notification` + сервис `TelegramNotifier`
    (на основе кода из `docs/24-exaple-sendphp-from-site.md`).
47. Cron-команды через планировщик Laravel: уведомления о новой заявке,
    о задачах (сегодня/просрочены), о мероприятиях (за неделю, за 3 дня,
    в день).
48. Отправка через database-очередь, без постоянного воркера.
49. Feature-тесты команд уведомлений (с моком HTTP-запросов к Telegram).

### Фаза 9 — Дашборд
50. Виджеты дашборда: показатели MVP из `docs/15-dashboard-and-analytics.md`
    (заявки, сделки по этапам, суммы, конверсия, ближайшие мероприятия
    и т. д.) с фильтрами периода (дизайн по референсу 4).
51. Тесты корректности агрегаций дашборда.

### Фаза 10 — PWA и push
52. `manifest.json`, иконки, проверка установки на iPhone через Safari.
53. Простой service worker (кэш статики/app shell).
54. Мобильная адаптация всех экранов: канбан касанием, таблицы,
    календарь без горизонтальной прокрутки. Здесь это итоговая сквозная
    проверка на iPhone в обеих темах: мобильный вид проверяется на каждой
    дизайн-задаче раньше (`docs/26-ui-designe-plan.md`).
55. Миграция и модель `PushSubscription`, подписка на Web Push,
    отправка через `minishlink/web-push` из тех же cron-команд, что и
    остальные уведомления.
56. Ручной чек-лист проверки push и установки PWA на реальном iPhone
    (не автоматизируется).

### Фаза 11 — Продакшен
57. Прогон чек-листа совместимости хостинга из
    `docs/23-hosting-constraints.md` на реальном REG.RU (версия PHP,
    Composer, расширения, лимиты).
58. Настройка `.env` на продакшене, cron-бэкап БД и каталога документов.
59. Ручной и автоматический прогон критериев приёмки из
    `docs/20-acceptance-criteria.md`.
60. Финальный аудит безопасности по `docs/19-security.md` перед первым
    релизом.

---

## Статус выполнения

| Фаза | Задачи | Статус |
|---|---|---|
| 0 — Bootstrap | 1–5 | выполнена |
| 1 — Пользователи и доступ | 6–12 | выполнена |
| 1а — Каркас интерфейса | 12.1, 12.2 | выполнена (проверено на iPhone) |
| 2 — Клиенты | 13–19 | выполнены |
| 2 — Клиенты | Д1 (Д1а — дизайн-система: токены, компоненты, витрина; Д1б — перенос экранов) | выполнена; попутно — деактивация приглашений (`docs/03`) |
| 2 — Клиенты | Д2 (страница входа, регистрации, «Ссылка недействительна»; логотип) | выполнена |
| 2 — Клиенты | 19а, Д3 (дизайн), затем 20 (итоговые тесты) | не начаты; порядок и референсы — `docs/26-ui-designe-plan.md` |
| 3–11 | 21–60 | не начаты |

Промпты для Claude Code по выполненным фазам — `docs/prompts/`
(`01-bootstrap.md`, `02-users-and-access.md`, `02b-layout.md`,
`03-clients.md`). При расхождении промпта и реализации актуально описание в
профильных документах (`docs/08`, `docs/17`, `docs/18` и т. д.).

Известные решения, принятые по ходу работы (уже отражены в документах):
- две роли (создатель — единственный, участник), приглашение по ссылке на
  24 часа, восстановление пароля после MVP (`docs/03`, `docs/21`);
- клиент: обязательно только имя, российский телефон `+7XXXXXXXXXX`,
  юридический тип из списка, роль свободным текстом, слияние доступно всем
  (`docs/08`, `docs/18`);
- интерфейс: левое меню, нижняя панель на телефоне, стрелка «Назад» в
  родительский раздел, правила вёрстки для iPhone (`docs/16`, `docs/17`);
- для будущих фаз: перенос сделок, задач и документов при слиянии
  добавляется в `ClientMergeService::relations()`; заявки с лендинга
  используют `PhoneNormalizer` и `DuplicateFinder` (`docs/13`, `docs/18`).
